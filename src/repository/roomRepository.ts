import Room from "../models/room";
import User, {UserState} from "../models/user";

export class RoomRepository {
    // public async getRoomByUserId(userId: number): Promise<User | null> {
    //     return await Room.find({
    //         where: {
    //             userId,
    //         },
    //     });
    // }
}
